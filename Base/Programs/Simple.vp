!!ARBvp1.0


DP4		result.position.x, program.local[0], vertex.position;
DP4		result.position.y, program.local[1], vertex.position;
DP4		result.position.z, program.local[2], vertex.position;
DP4		result.position.w, program.local[3], vertex.position;

END
